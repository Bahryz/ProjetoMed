name: medico_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Firebase Core atualizado para compatibilidade
  firebase_core: ^3.13.1
  firebase_analytics: ^11.4.6
  go_router: ^15.1.2
  provider: ^6.1.5
  firebase_auth: ^5.5.4
  cloud_firestore: ^5.6.8
  mask_text_input_formatter: ^2.9.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

